export class NuevoUsuario {

    nombre!: string;   // El signo de exclamación es para que no llore cuando las variables no están unicializadas
    nombreUsuario!: string;
    email!: string;
    password!: string;
    authorities!: string[];
}
